  // X=24, Y=25, Z=26, AA=27, AB=28
  // Check if columns exist before hiding (fix for "out of bounds" error)
  try {
    const maxCol = sh.getMaxColumns();
    if (maxCol >= 28) {
      sh.hideColumns(24, 5);
    } else {
      // Expand sheet to have enough columns, then hide
      sh.insertColumnsAfter(maxCol, 28 - maxCol);
      sh.hideColumns(24, 5);
    }
  } catch (e) {
    // If hiding fails, log but don't crash
    Logger.log("Could not hide helper columns: " + e.message);
  }
